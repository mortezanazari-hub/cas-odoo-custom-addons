# UI Review Cycle 9 — Iteration 10

## تغییرات

- نام و نشان «فضای کار سازمانی / CAS Workspace» در Sidebar به کنترل قابل کلیک تبدیل شد و همیشه Route میزکار (`home`) را باز می‌کند.
- نشان وضعیت حضور در Hero میزکار از کارت مستقل دارای دکمه جزئیات به یک نشان کوچک، کم‌کنتراست و یکپارچه با هدر تبدیل شد.
- کل نشان حضور قابل کلیک است و صفحه «حضور و شیفت» را باز می‌کند.
- رفتار نمایش در موبایل و Sidebar جمع‌شده نیز اصلاح شد.

## اثر Backend

بدون تغییر Backend؛ تغییر صرفاً در Navigation و UI Workspace است.
