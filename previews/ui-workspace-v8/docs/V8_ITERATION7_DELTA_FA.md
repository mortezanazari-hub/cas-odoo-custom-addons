# نسخه ۸ — Iteration 7

## تصمیم محصولی

- صفحه مستقل «جست‌وجوی سازمان» حذف شد.
- صفحه مستقل «تاریخچه اخیر» حذف شد.
- Routeهای `global-search-page` و `recent-history` از Navigation و Route Renderer حذف شدند.
- جست‌وجوی سازمانی به Modal/Command Palette تبدیل شد.
- تاریخچه اخیر فقط در حالت خالی بودن عبارت جست‌وجو داخل همان Modal نمایش داده می‌شود.

## رفتار Modal

- فراخوانی از Hero یا `Ctrl + K`
- بستن با `Esc`، دکمه بستن یا کلیک روی Overlay
- Autofocus روی ورودی
- فیلتر تک‌انتخابی نوع نتیجه
- گروه‌بندی اشخاص، اقدام‌ها، نامه‌ها، اسناد و فرم‌ها
- نمایش موارد اخیر پیش از تایپ
- نتایج محدود و مناسب اتصال به جست‌وجوی Server-side

## امنیت Production

هر Provider باید قبل از بازگرداندن عنوان و Metadata، ACL، Record Rule، Company Scope و دسترسی عملیات را بررسی کند. History نیز هنگام نمایش مجدد باید مجوز رکورد منبع را دوباره ارزیابی کند.

> اصلاح پس از تأیید Iteration 7: مشکل Auto-scroll سراسری در `V8_ITERATION8_DELTA_FA.md` ثبت و رفع شده است.
